name: lcu_connector
description: A library to connect in LCU client API.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.14.4 <3.0.0"

dependencies:
  event_listener: ^0.2.0
  http: ^0.13.4

dev_dependencies:
  test: ^1.19.4
